您好，我们在上面讨论过关于“降低显存、花小钱办大事”的结构感知方案，请问其中您提出的方案一，即 Patch Graph / kNN 关系建模的方案，请您作为工业图像异常检测领域的专家，请您就该方案具体展开讲解一下呗，谢谢

###### ChatGPT 说：

非常好，这个问题**已经完全进入“方法设计核心层”**了。  
下面我会**系统、从零到可落地**地把 **Patch Graph / kNN 关系建模** 这一方案讲清楚，目标是让你在听完后：

* ✅ 知道它**在异常检测里解决什么问题**

* ✅ 明白它**为什么比全局 self-attention 划算**

* ✅ 能直接把它**嵌进你现在的 CKAAD / GAN / 重构框架**

* ✅ 知道它**对哪些 MVTec 异常类型最有效**

我会尽量用你已经熟悉的概念，不引入“为了炫技而炫技”的图神经网络术语。

* * *

一、Patch Graph / kNN 关系建模到底在解决什么？
================================

我们先回到**你最核心的目标**：

> **让模型具备判断：  
> “这个 patch 出现在这里，是否在整体结构上是合理的？”**

这是一个**关系问题**，不是外观问题。

* * *

1️⃣ 为什么“单 patch 判别”一定不够？
------------------------

在 MVTec 中，有一大类异常：

* transistor::misplaced

* metal_nut::flip

* zipper::split_teeth

* screw::thread_top

它们的共同点是：

> **单个 patch 看起来完全正常，但 patch 与 patch 之间的关系错了**

所以任何只做：

* 通道注意力

* 空间注意力

* 重构误差

的方法，都会**天然失效**。

* * *

2️⃣ Patch Graph 的核心思想（一句话）
--------------------------

> **不要问“这个 patch 像不像正常”，  
> 而要问“它和它应该相邻的 patch，关系对不对”。**

* * *

二、Patch Graph 的基本构成（非常直观）
=========================

我们一步一步来。

* * *

Step 1：把特征图变成 patch token
-------------------------

假设你在 backbone 的某一层，得到特征：

F∈RC×H×W

我们把它 reshape 成：

{fi​}i=1N​,N=H×W

其中：

* 每个 fi​ 是一个 patch 的特征向量

* patch 的空间位置 (xi​,yi​) 是已知的

📌 **注意**：  
这里的 patch 是“特征 patch”，不是像素 patch，**计算量很可控**。

* * *

Step 2：构建 kNN Patch Graph（关键）
-----------------------------

### 核心问题：

> 对于 patch i，哪些 patch 是它“应该参考的对象”？

### 两种常见 kNN 定义方式（工业 AD 推荐第 1 种）

#### ✅ 方式 A：**特征相似 kNN（最常用）**

* 在特征空间里：

kNN(i)=argjmin​∥fi​−fj​∥

含义是：

> “在正常情况下，和我长得最像的 patch，应该满足某种结构关系。”

这对：

* 周期结构（thread / teeth）

* 重复部件（bristles）  
  非常有效。

* * *

#### ⚠️ 方式 B：空间邻域 kNN（辅助）

* 只在一定空间半径内建边

* 更偏向“连通性建模”

* * *

### 图的结果是什么？

你得到了一个**稀疏图**：

* 节点：patch

* 边：k 个最相关 patch

📌 **显存关键点**

* 边数是 N⋅k

* 而不是 N2（自注意力）

* * *

Step 3：在 Patch Graph 上做“关系检查”
-----------------------------

这一步有多种实现方式，**不一定要用复杂 GNN**。

我给你三个从“轻 → 重”的版本。

* * *

### 🥇 版本 1（最轻，最稳）：关系一致性度量（强烈推荐起步）

对每个 patch i，计算：

ri​=k1​j∈kNN(i)∑​∥fi​−fj​∥

直觉解释：

* 正常结构：
  
  * 相邻/对应 patch 特征高度一致
  
  * ri​ 小

* 结构异常：
  
  * misplaced / split
  
  * patch 虽然“像正常”
  
  * 但和它应该对应的 patch **不一致**
  
  * ri​ 大

👉 **这个 ri​ 本身就是一个结构异常分数**

几乎不增加参数，也不容易炸训练。

* * *

### 🥈 版本 2：Graph Message Passing（中等复杂度）

你可以做一层：

f~​i​=fi​+j∈kNN(i)∑​wij​⋅fj​

然后看：

∥fi​−f~​i​∥

含义是：

> “如果我是正常结构，我能被邻居合理解释；  
> 如果解释不了，我可能是结构异常。”

* * *

### 🥉 版本 3：Graph Attention（最强，但稍复杂）

* 对每条边学一个权重

* 类似 self-attention，但只在 kNN 子集上

📌 实践经验：

> 工业 AD 中，**版本 1 或 2 往往就够了**。

* * *

三、Patch Graph 为什么“花小钱办大事”？
==========================

1️⃣ 显存复杂度对比（再强调一次）
------------------

| 方法                | 复杂度    |
| ----------------- | ------ |
| 全局 self-attention | O(N2)  |
| Patch Graph (kNN) | O(N⋅k) |

举例：

* N = 1024

* k = 8

👉 **省了 ~128 倍的显存**

* * *

2️⃣ 为什么它特别适合工业异常检测？
-------------------

因为工业图像有几个特点：

1. **结构稳定**

2. **重复模式多**

3. **异常是“偏离结构”而不是“新结构”**

Patch Graph 恰好就是在问：

> “你有没有偏离你应该遵守的那一小群关系？”

* * *

四、Patch Graph 对不同异常类型的作用
========================

这是你写论文时**非常重要的一张认知表**：

| 异常类型             | Patch Graph 是否有效 | 原因           |
| ---------------- | ---------------- | ------------ |
| scratch / stain  | ❌                | 外观异常，不是关系异常  |
| crack / cut      | ⚠️               | 可检测断裂，但不是最优  |
| rough            | ❌                | 全局统计漂移       |
| broken_teeth     | ✅                | 缺失破坏了关系      |
| split_teeth      | ✅✅               | 关系错位，极其典型    |
| misplaced        | ✅✅               | patch 正常但位置错 |
| missing bristles | ✅                | 对应关系消失       |

* * *

五、如何和你现有的 CKAAD / GAN 框架结合？
===========================

给你一个**最小侵入式集成方案**。

* * *

✅ 推荐集成点
-------

1. **不要改 encoder**

2. 在 encoder 输出的 feature map 上：
   
   * 构建 Patch Graph
   
   * 计算结构异常分数 Sstruct​

3. 最终 anomaly score：

S=αSCKAAD​+(1−α)Sstruct​

* * *

训练时怎么用？
-------

### 两种方式都可：

* **Inference-only（最稳）**
  
  * 不反向传播
  
  * 直接作为后处理

* **Weakly-supervised 结构一致性 loss**
  
  * 只在正常图上
  
  * 强化“正常 patch 的关系一致性”

* * *

六、你可以直接写进论文的方法描述（示意）
====================

> We construct a sparse patch graph based on k-nearest neighbors in the feature space and quantify structural inconsistency by measuring the deviation between a patch and its relational neighborhood. This enables efficient structural reasoning without incurring the quadratic cost of global self-attention.

* * *

七、给你一个非常现实的研究建议
===============

> **不要一开始就做“图神经网络”**  
> 先做：

* kNN graph

* 关系一致性度量

你会发现：

* misplaced / split_teeth 的 Pixel-PRO **明显涨**

* 代码量和显存几乎不变

* 审稿人也很容易接受

* * *

如果你愿意，下一步我可以帮你：

1. **写一个 PyTorch 级别的 kNN Patch Graph 伪代码**

2. 帮你选 **在哪一层特征上做 Graph 最划算**

3. 帮你设计一个 **只用正常样本训练的结构一致性 loss**

你现在这个方向，**是非常正确且很有潜力的**。

##### 你说：

您好，请您先按照上面的您提出的下一步的三个问题方向详细阐述一下呗，然后阐述之后我还有两个问题想向您请教一下，请您先给我讲解一下以上您提出的三个方向

###### ChatGPT 说：

下面我就按你要的 **3 个“下一步方向”**逐一**详细展开**。我会尽量做到：**可直接落地到你的 CKAAD 代码库**、并且能解释**为什么会提升 Pixel-AUROC / Pixel-PRO**。  
（你后面两个问题你再问，我再接着答。）

* * *

方向 1：给你一套 PyTorch 级别的 kNN Patch Graph 实现思路（含伪代码）
================================================

1.1 输入与输出你要定义清楚
---------------

你通常在某一层拿到特征图：

* `F: [B, C, H, W]`

你要输出：

* `S_struct: [B, 1, H, W]`（每个 patch 的结构异常分数）

* 最终与原 pixel map 对齐时再上采样到输入分辨率

1.2 Patch token 化
-----------------

把特征图变成 token：

* `X: [B, N, C]`，其中 `N = H*W`

`def feat_to_tokens(F):  # F: [B,C,H,W]     B,C,H,W = F.shape    X = F.flatten(2).transpose(1,2).contiguous()  # [B, N, C]     return X, H, W`

建议对 token 做归一化（更稳）：

`X = F.normalize(X, dim=-1)`
1.3 kNN 构图（核心）
--------------

### 方案 A：全局 kNN（简单，但 O(N^2) 计算）

对 MVTec 常见的 H=W=32（N=1024）在单层上还算能跑，但不够优雅。

更推荐：

### 方案 B：**局部窗口内 kNN（省算力 + 更稳定）**

先限定候选邻居集合，再 kNN。工业场景里结构异常往往是“局部关系错”，局部窗口足够。

#### 伪代码（局部窗口 kNN）

`# X: [B, N, C], pos: [N,2]  (x,y) # 对每个 token i，只在以 i 为中心的 r×r 窗口里找 kNN neighbors = [] for i in range(N):    cand = window_indices(i, r)          # 长度 M，M << N     sim = X[:, i:i+1, :] @ X[:, cand, :].transpose(-1,-2)  # [B,1,M]     idx = sim.topk(k, dim=-1).indices    # [B,1,k]     neighbors.append(cand[idx])`

> 工程上你会做向量化（不写循环），但逻辑就是这个。

1.4 结构异常分数怎么定义？（给你 3 种强烈推荐的形式）
------------------------------

### 形式 1（最稳、最轻）：**邻域一致性残差**

ri​=k1​j∈kNN(i)∑​∥fi​−fj​∥2​

实现：

`# Xi: [B,N,C], neigh: [B,N,k] -> gather 得到 Xj: [B,N,k,C] diff = (Xi.unsqueeze(2) - Xj).pow(2).sum(-1).sqrt()  # [B,N,k] r = diff.mean(dim=2)  # [B,N] S_struct = r.view(B,1,H,W)`

直觉：

* 正常结构：相似 patch 很一致 ⇒ r 小

* 关系/错位/缺失：邻域解释不了 ⇒ r 大

### 形式 2（更“结构化”）：**邻域重构误差**

先用邻域聚合得到一个“结构预测”：

f~​i​=j∑​wij​fj​

再算误差：

si​=∥fi​−f~​i​∥

这更接近“结构先验下的重构”。

### 形式 3（最强但更复杂）：Graph Attention（kNN 版本）

只在 kNN 上做 attention，不做 N×N：

wij​=softmax(qi⊤​kj​)

显存从 N2 降到 N⋅k。

> 你起步建议先用形式 1，因为它几乎不会“训炸”。

* * *

方向 2：在哪一层特征上做 Graph 最划算？（这是决定效果与显存的关键）
======================================

你在 CKAAD 里通常会有多尺度特征（浅/中/深），选择不同层会直接影响：

* patch 数 N（影响显存）

* 特征语义（影响“关系异常”可见性）

* 对 Pixel-PRO 的边界形态

我给你一个“工业 AD 最常用”的选型结论：
2.1 三层特征的差异（非常实用）
-----------------

### 浅层（高分辨率，纹理边缘强）

* 优点：对 crack/scratch 边缘敏感

* 缺点：对 misplaced / flip 的“关系语义”弱

* 代价：N 大，kNN 更贵

**适合：结构断裂/线状异常**（crack、cut、scratch）

* * *

### 中层（平衡层，推荐首选）

* 优点：保留空间结构 + 有一定语义

* 对 zipper teeth / screw thread / toothbrush bristles 很敏感

* 代价：N 中等（例如 32×32）

**适合：周期结构异常 + 部件排列异常 + 一部分关系异常**

✅ **我建议你把 Graph 放在中层作为主干**

* * *

### 深层（低分辨率，语义强）

* 优点：对 misplaced/flip/split_teeth 最有希望

* 缺点：定位边界会糊（PRO 可能下降）

* 代价：N 小，非常省显存

**适合：关系异常 / 部件存在性（缺失）**

* * *

2.2 最推荐的工业 AD 配置（强烈建议你这样做）
--------------------------

> **双层 Graph（1 个深层 + 1 个中层）**

* 深层：负责关系异常（misplaced/flip）

* 中层：负责周期/局部结构（thread/teeth/bristles）

* 最后融合时：
  
  * 深层 score 上采样 + 边界 refinement（用中层引导）

这样会同时照顾：

* Pixel-AUROC（异常区分度）

* Pixel-PRO（区域形态）

* * *

方向 3：只用正常样本训练的“结构一致性 loss”怎么设计？
===============================

这一步非常关键，因为它能让你的结构分数不只是“后处理”，而是变成**模型能力的一部分**。

你要的原则是：

* 不需要异常标签

* 不需要额外数据

* 能逼迫模型在正常图上学会“结构关系”

下面给你 3 类很常用、非常可写论文的 loss。

* * *

3.1 Loss A：邻域一致性约束（最简单最稳）
-------------------------

在正常样本上，最小化：

Lstruct​=E​N1​i∑​k1​j∈kNN(i)∑​∥fi​−fj​∥2​​

含义：

> 正常结构下，相似 patch 的关系应当稳定一致。

优点：

* 极简单

* 不容易破坏主任务

* 对周期结构提升明显

缺点：

* 可能过平滑（要加“负样本”或用 stop-grad 防塌陷）

* * *

3.2 Loss B：对比式结构一致性（更强）
-----------------------

你构造正负关系：

* 正样本：kNN(i)

* 负样本：随机远处 patch / 不同结构簇 patch

用 InfoNCE：

Li​=−logexp(sim(fi​,fj+​)/τ)+∑j−​exp(sim(fi​,fj−​)/τ)exp(sim(fi​,fj+​)/τ)​

含义：

> 正常图里，“正确关系”应该比“错误关系”更相似。

优点：

* 能防塌陷

* 对 misplaced/flip 更有帮助

缺点：

* 工程略复杂一点

* * *

3.3 Loss C：结构预测（自监督）——“从邻域预测我是谁”
--------------------------------

做一个小 head：

f^​i​=g({fj​}j∈kNN(i)​)

最小化：

∥fi​−f^​i​∥

含义：

> 正常情况下，我应该能被邻居预测出来；异常时预测失败。

优点：

* 特别适合周期结构与规则排列

* 直接对应“结构可解释性”

* * *

3.4 这些 loss 怎么跟 CKAAD 融合？（建议方式）
-------------------------------

你不要用一个很大的权重直接压住原来的损失，建议：

* `L_total = L_CKAAD + λ * L_struct`

* λ 从 **0.01 或 0.05** 起步（非常小）

* 只在你选定的那一层/两层特征上施加

这样最稳定。

* * *

最后：把三条方向合成一个“最小可实现方案”（你可以照这个做）
==============================

1. 选中层特征（比如 32×32）

2. 构建局部窗口 kNN 图（k=8 或 16）

3. 用“邻域一致性残差”得到 `S_struct`（无需训练即可做推理）

4. 推理阶段融合：
   
   * `S = α*S_CKAAD + (1-α)*normalize(S_struct)`

5. 如果要训练：
   
   * 只用正常样本加一个 `L_struct`（λ 小）

* * *

你先消化这三部分。  
然后你说你还有两个问题——你直接问，我会在这套框架基础上继续往下讲。

##### 你说：

您好，就是在这套框架当中，您首先对于 在Patch Graph的基本构成中提及的Step2和Step3中有一些疑问：“就是，我应该如何根据MVTecAD数据当中不同数据集的特点，例如轴对称、中心对称图形中的patch关系，来形成最为合理的knn计算建模和关系检查公式呢？

###### ChatGPT 说：

这个问题问得非常到位：**Patch Graph 的 Step2（怎么建边）和 Step3（怎么检查关系）**如果不利用工业数据的先验（轴对称/中心对称/周期结构），就容易变成“通用 kNN”，效果会打折；但如果你把这些先验用进去，kNN 图会更“物理合理”，对 **misplaced / flip / split_teeth / thread_top** 这类关系异常特别有效。

下面我给你一个**可直接落地的设计框架**：把“对称/周期/装配关系”转成 **邻接规则 + 关系评分公式**。你可以按类别选择一套或多套规则组合。

* * *

1) 先统一一个思路：kNN 不是只能“按特征相似”

--------------------------

在工业 AD 里，kNN 最合理的形式通常是**“特征相似 + 结构先验约束”的混合图**：

N(i)=外观相似kNNfeat​(i)​​ ∪ 局部邻域kNNpos​(i)​​ ∪ 对称伙伴kNNsym​(i)​​ ∪ 周期/相位kNNper​(i)​​

然后 Step3 的“关系检查”也不只一个公式，而是按边类型分别检查，再融合：

si​=wf​si(feat)​+wp​si(pos)​+ws​si(sym)​+wper​si(per)​

这就是你要的“根据数据特点，形成最合理的 kNN 建模与检查公式”的核心套路。

* * *

2) Step2：如何用“轴对称/中心对称”去建图

-------------------------

### 2.1 先把 patch 坐标标准化（很关键）

假设特征图是 H×W，patch i 的网格坐标是 (xi​,yi​)（0…W-1, 0…H-1）。  
把它变为以中心为原点的归一化坐标：

ui​=(W−1)/2xi​−(W−1)/2​,vi​=(H−1)/2yi​−(H−1)/2​

后面所有“对称映射”都在 (u,v) 上定义最方便。

* * *

### 2.2 轴对称（左右/上下镜像）

* **左右镜像**（关于 y 轴）：

TLR​(u,v)=(−u,v)

* **上下镜像**（关于 x 轴）：

TUD​(u,v)=(u,−v)

把对称后的坐标映射回最近的 patch 索引 j=T(i)。  
然后在 Step2 里**强制把对称伙伴加入邻居集合**：

Nsym​(i)={TLR​(i),TUD​(i)}∩valid

> 适用：**transistor（引脚左右对称）、metal_nut（翻转导致对称关系破坏）**等。

* * *

### 2.3 中心对称（180° 旋转）

中心对称映射：

TC​(u,v)=(−u,−v)

同样加入：

Nsym​(i)={TC​(i)}

> 适用：**很多近似中心对称的零件/纹理对象**（某些 metal_nut、瓶盖类结构、对称装配件）。

* * *

### 2.4 旋转对称（k 折对称）——更高级但很强

像拉链齿、螺纹、某些圆形零件可能存在近似旋转规律。  
你可以定义一组旋转变换（角度 θm​=2πm/K）：

Tθ​(u,v)=(ucosθ−vsinθ, usinθ+vcosθ)

将 Tθ​(i) 对应的 patch 索引加入邻居。

> 实际工程里通常不需要全做，**K=2（中心对称）最常见**；K>2 在 MVTec 少一些，但在圆形重复结构上很好用。

* * *

3) Step2：周期结构（thread/teeth/bristles）如何建图

----------------------------------------

周期结构的“合理邻居”通常不是“长得像”，而是**同一方向上的相位邻居**。

### 3.1 先确定主方向（无需太复杂）

两种常用办法：

* 直接用先验：例如 screw thread 多为某个固定方向（数据基本一致）

* 或在特征图上做个简单的方向估计（结构张量/梯度主方向）

得到方向单位向量 d=(dx​,dy​)。

### 3.2 用“相位邻居”建边

对每个 patch i，定义沿方向 ±d 的几个步长：

j=i+t⋅d,t∈{1,2,…,T}

离散到最近 patch index。  
把这些加入 Nper​(i)。

> 适用：**zipper teeth、screw thread、toothbrush bristles、tile 规则纹理**  
> 对 **thread_top / split_teeth / squeezed_teeth** 特别有效。

* * *

4) Step3：关系检查公式怎么“对症下药”

-----------------------

你提到“关系检查公式”想针对对称结构来设计——这正是提升的关键。下面给你三类最常用、最有效的检查方式。

* * *

### 4.1 对称一致性检查（最直接）

如果 j=T(i) 是对称伙伴，正常情况下两者特征应当相似（允许一定差异）。定义：

si(sym)​=∥fi​−fT(i)​∥2​

如果是左右对称/中心对称对象，**flip / misplaced** 会显著把这个量拉大。

> 这对你关心的“patch 在此处是否合理”非常直接：  
> 因为“合理”意味着它应当与对称位置的对应部件匹配。

**小技巧（更稳）**：用通道归一化或 cosine 距离：

si(sym)​=1−∥fi​∥∥fT(i)​∥fi⊤​fT(i)​​

* * *

### 4.2 相对位移一致性检查（适合 misplaced / 装配关系）

仅仅要求“相似”还不够，有时你想要“位置关系也对”。  
比如 transistor 引脚间距、zipper 左右齿咬合关系等。

你可以对每条边 (i,j) 建模“正常的相对位移模式”。最简单做法：

* 训练时在正常图统计同类边的相对位移 Δpij​=pj​−pi​

* 得到均值与协方差 (μ,Σ)

* 测试时计算马氏距离作为关系异常分数：

sij(pos)​=(Δpij​−μ)⊤Σ−1(Δpij​−μ)

最终 patch 分数取邻边平均：

si(pos)​=∣N(i)∣1​j∈N(i)∑​sij(pos)​

> 这个公式的意义非常强：  
> “我和我的结构伙伴之间的相对几何关系是否仍符合正常装配规则”。

* * *

### 4.3 周期相位一致性检查（适合 thread/teeth）

对周期结构，最有效的是检查“沿主方向的局部一致性”：

si(per)​=T1​t=1∑T​∥fi​−fi+td​∥2​

以及检查“相位断裂”：如果某个位置周期突然不连续，si(per)​ 会尖峰。

> 对 **thread_top、broken_teeth、split_teeth** 都很敏感。  
> 特别是 split_teeth：每个齿 patch 可能都“正常”，但**两侧相位对应关系错**，这个分数会很明显。

* * *

5) 工业数据里“对称不是完美对称”，怎么处理？

------------------------

这是很现实的问题：光照、透视、材质导致对称位置不完全一样。

你可以用两招让对称关系更鲁棒：

### 5.1 只在“同部件区域”做对称检查

比如 transistor 的引脚区域 vs 外壳区域不要混。  
做一个粗 mask（甚至用阈值/边缘就行）把区域分开再对称。

### 5.2 用“对称-邻域”而非“对称-单点”

把 T(i) 扩展成一个小窗口 Ω(T(i))，取最相似的点：

si(sym)​=j∈Ω(T(i))min​∥fi​−fj​∥

这样就允许对称位置有轻微错位（非常常见）。

* * *

6) 最推荐的“组合配方”（你可以直接用在 MVTec 上）

------------------------------

如果你不想为每个类别手工设计太多，给你一个普适但有效的组合：

1. **基础邻居**：局部窗口邻居 Npos​（保证连通性/PRO 形态）

2. **特征 kNN**：Nfeat​（保证对重复结构敏感）

3. **对称边（可选开关）**：Nsym​（只对明显对称的类启用）

4. **周期边（可选开关）**：Nper​（对 thread/teeth/bristles 启用）

关系检查用：

* si(feat)​=mean∥fi​−fj​∥

* si(sym)​=∥fi​−fT(i)​∥ 或 min-window

* si(per)​=mean∥fi​−fi+td​∥

最后融合：

si​=αsi(feat)​+βsi(sym)​+γsi(per)​

并把 si​ reshape 回 [H,W] 做上采样即可。

* * *

7) 你下一步最该做的两件“小实验”

------------------

为了把这套“对称/周期建图”变成论文里可信的贡献，我建议你做两个 ablation：

1. **只用 feature kNN vs feature kNN + symmetry edges**  
   看 transistor/metal_nut/zipper 的 Pixel-PRO 是否明显涨（特别是 misplaced/flip/split_teeth）

2. **只用 feature kNN vs feature kNN + periodic edges**  
   看 screw/zipper/toothbrush 的 thread/teeth/bristles 类异常是否更稳

这两个实验特别容易证明“结构先验建图”的价值。

* * *

如果你愿意，我可以再进一步把它落到你当前工程上：  
你告诉我你打算在哪一层特征（比如 32×32 or 16×16）做 graph，以及你主要想先攻的类别（transistor/zipper/screw 哪个优先），我就能帮你给出：

* k、窗口半径 r、周期步长 T 的推荐值

* 哪些类别开 symmetry / periodic 开关

* 以及怎么把这些开关写成“自动策略”（不需要人工指定类名）
