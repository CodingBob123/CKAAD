# 异常检测结果可视化指南

## 🎯 功能概述

本文档介绍如何在MVTecAD数据集上训练完成后，对测试样本进行anomaly map可视化，展示模型检测到的异常区域。

## 📊 可视化内容

每个可视化结果包含：
1. **原始图像**：测试样本的原始图像
2. **Anomaly Map**：模型预测的异常热力图（红色区域表示异常程度高）
3. **Ground Truth**：真实标注的异常区域（如果有标注）

## 🔧 实现方式

### **自动可视化（推荐）**

训练完成后会自动进行可视化：

```bash
# 训练脚本会自动调用可视化功能
python main.py --dataset mvtec --normal transistor [其他参数]

# 可视化结果保存在：
# ./results/mvtec_transistor_final_epoch_XXX/
```

### **手动可视化**

如果需要手动进行可视化，可以在训练后单独调用：

```python
from main import visualize_anomaly_maps_simple
from util.test import evaluation_pixel

# 加载训练好的模型
# pfe = PretrainedFeatureExtractor(...)
# ae = ED(...)

# 获取测试数据
# test_dataloader = ...

# 进行可视化
visualize_anomaly_maps_simple(pfe, ae, test_dataloader, args, device, epochs)
```

## 📁 输出文件结构

```
./results/
├── mvtec_transistor_final_epoch_200/
│   ├── sample_000_label_0.png    # 正常样本
│   ├── sample_001_label_0.png    # 正常样本
│   ├── sample_002_label_1.png    # 异常样本 (类别1)
│   ├── sample_003_label_2.png    # 异常样本 (类别2)
│   └── ...
```

## 🎨 可视化说明

### **颜色编码**
- **原始图像**：RGB彩色图像
- **Anomaly Map**：Jet色图
  - 蓝色：异常程度低（正常区域）
  - 红色：异常程度高（异常区域）
- **Ground Truth**：灰度图
  - 白色：异常区域
  - 黑色：正常区域

### **文件名含义**
- `sample_XXX`：样本序号
- `label_Y`：异常类别标签（0=正常，其他=异常类别）

## 🔍 Anomaly Map计算原理

Anomaly Map通过以下步骤计算：

1. **特征提取**：
   ```python
   inputs = pfe(img)      # 编码器提取多尺度特征
   outputs = ae(inputs)   # 自编码器重建特征
   ```

2. **相似度计算**：
   ```python
   # 对每个尺度计算余弦相似度
   anomaly_map = 1 - F.cosine_similarity(fs, ft)
   ```

3. **多尺度融合**：
   ```python
   # 将不同尺度的anomaly map相乘融合
   anomaly_map *= a_map  # 累乘得到最终结果
   ```

4. **上采样**：
   ```python
   # 上采样到原始图像尺寸
   anomaly_map = F.interpolate(anomaly_map, size=img_size, mode='bilinear')
   ```

## 📈 可视化效果评估

### **优秀检测效果**
- Anomaly Map的红色区域与Ground Truth高度重合
- 异常区域边界清晰，定位准确
- 正常区域显示为蓝色，无误检

### **需要改进的方面**
- 如果Anomaly Map在正常区域出现红色，可能存在误检
- 如果异常区域没有明显红色高亮，可能存在漏检
- 如果边界模糊，可能需要调整特征融合策略

## 🛠️ 自定义可视化参数

### **修改可视化样本数量**
```python
# 在main.py的visualize_anomaly_maps_simple函数中
if cnt >= 10:  # 修改这个数字来控制可视化样本数量
    break
```

### **修改图像尺寸和布局**
```python
# 调整子图布局
fig, axes = plt.subplots(1, 3, figsize=(15, 5))  # 更大的图像
# 或
fig, axes = plt.subplots(2, 2, figsize=(10, 10))  # 2x2布局
```

### **自定义颜色映射**
```python
# 使用不同的colormap
axes[1].imshow(anomaly_map, cmap='hot', vmin=0, vmax=1)  # hot色图
# 或
axes[1].imshow(anomaly_map, cmap='plasma', vmin=0, vmax=1)  # plasma色图
```

## 🔧 故障排除

### **常见问题**

1. **matplotlib后端错误**：
   ```python
   # 在main.py开头添加
   import matplotlib
   matplotlib.use('Agg')  # 使用非GUI后端
   ```

2. **结果文件夹不存在**：
   - 程序会自动创建 `./results/` 文件夹
   - 确保有写入权限

3. **可视化样本过少**：
   - 检查测试数据加载器
   - 调整 `viz_indices` 的选择逻辑

4. **图像显示异常**：
   - 检查数据归一化和反归一化
   - 确认图像通道顺序 (C, H, W) vs (H, W, C)

### **性能优化**

1. **减少可视化开销**：
   ```python
   # 只可视化异常样本
   if labels[i] > 0:  # 只保存异常样本
       plt.savefig(save_path)
   ```

2. **批量处理**：
   ```python
   # 使用更大的batch_size加速
   viz_dataloader = torch.utils.data.DataLoader(viz_dataset, batch_size=8, shuffle=False)
   ```

## 📊 与评估指标的关系

### **Pixel-level AUROC**
- 衡量异常像素的分类准确性
- Anomaly Map的整体质量指标

### **Pixel-level PRO**
- 衡量异常区域的定位准确性
- 特别关注缺陷边界的精确定位

### **可视化与指标的关系**
- **清晰的边界** → 更高的PRO分数
- **准确的定位** → 更高的AUROC分数
- **适当的敏感度** → 平衡精确率和召回率

## 🎯 使用建议

1. **训练阶段**：定期可视化验证模型学习进度
2. **调参阶段**：通过可视化结果调整超参数
3. **分析阶段**：分析不同类别和不同缺陷类型的检测效果
4. **部署前**：验证模型在实际场景下的表现

## 📚 相关代码文件

- `main.py`: 主训练脚本，包含自动可视化功能
- `util/test.py`: 评估和可视化函数
- `visualize_anomaly_maps_simple()`: 简化的matplotlib可视化函数

---

通过这个可视化功能，你可以直观地观察模型的异常检测效果，分析不同类型缺陷的检测质量，为进一步的模型优化提供重要参考。

