# 结构感知模块使用说明

## ✅ 已完成的实现

### 1. Axial Attention 模块
- **文件位置**: `model/AxialAttention.py`
- **功能**: 轴向注意力模块，分别对Height和Width维度进行自注意力计算，增强patch间的结构感知能力
- **已在模型中集成**: ✅ 已在 `model/encoder.py` 的 `FusionLayer` 中引入

### 2. SSIM 损失函数
- **文件位置**: `main.py` 中的 `ssim_loss()` 函数
- **功能**: 结构相似度损失，用于衡量特征图的结构相似性
- **使用状态**: ✅ 已实现，默认未启用（代码中有详细注释说明如何启用）

---

## 📝 使用指南

### Axial Attention 的使用

#### 当前状态
Axial Attention **已经自动集成**到模型中，无需额外配置。它在 `FusionLayer` 的输出处自动应用。

#### 工作原理
1. 在 `encoder.py` 的 `FusionLayer.__init__()` 中：
   - 自动创建 `self.axial_attention` 模块
   - 通道数自动匹配：`input_channels[-1] * 2 * block.expansion`

2. 在 `FusionLayer.forward()` 中：
   - 在 `encode_layer1` 输出后自动应用轴向注意力
   - 增强特征的结构感知能力

#### 如需调整参数
编辑 `model/encoder.py` 第172行附近：
```python
self.axial_attention = AxialAttention(dim=axial_input_dim, heads=8, dim_head=64)
```
- `heads`: 多头注意力头数（默认8，可以改为4或16）
- `dim_head`: 每个头的维度（默认64）

---

### SSIM 损失的使用

#### 启用方法

**方法1：仅对最高分辨率特征层使用（推荐，计算快）**

在 `main.py` 的训练循环中（约第307行），找到：
```python
# ========== SSIM结构感知损失（可选，已实现但默认不启用）==========
```

取消下面这段代码的注释：
```python
# 【使用方法2：仅对最高分辨率特征层计算SSIM（更快，推荐先试）】
if len(normal_outputs) > 0:
    feat_pred = normal_outputs[0]  # 最高分辨率层（通常是第一层）
    feat_target = normal_inputs[0]
    # 如果特征图尺寸合适（>=11），直接计算；否则上采样
    if feat_pred.shape[-1] >= 11:
        # 仅使用第一个通道计算SSIM（加快计算）
        ssim_loss_value = ssim_loss(feat_pred[:, 0:1], feat_target[:, 0:1], 
                                    window_size=min(11, feat_pred.shape[-1]), size_average=True)
    else:
        # 上采样到合适尺寸
        target_size = max(32, feat_pred.shape[-1] * 2)  # 至少32x32
        feat_pred_up = F.interpolate(feat_pred[:, 0:1], size=(target_size, target_size), 
                                   mode='bilinear', align_corners=False)
        feat_target_up = F.interpolate(feat_target[:, 0:1], size=(target_size, target_size), 
                                      mode='bilinear', align_corners=False)
        ssim_loss_value = ssim_loss(feat_pred_up, feat_target_up, window_size=11, size_average=True)
    
    # 组合损失（权重可调）
    lambda_ssim = 0.1  # 建议从0.05开始，逐步调整到0.1-0.2
    recon_loss = recon_loss + lambda_ssim * ssim_loss_value
```

**方法2：对所有层级特征使用（效果更好，但计算量大）**

取消使用方法1的注释代码，该代码会对所有层级特征分别计算SSIM损失。

#### 参数调整建议

1. **lambda_ssim（SSIM损失权重）**
   - 建议从 `0.05` 开始尝试
   - 如果训练稳定，可以逐步增大到 `0.1` 或 `0.2`
   - 如果训练不稳定或损失过大，减小到 `0.01` 或 `0.02`

2. **window_size（高斯窗口大小）**
   - 默认 `11`，必须是奇数
   - 如果特征图很小（如8x8），可以改为 `7` 或 `5`
   - 在代码中会自动调整：`window_size=min(11, feat_pred.shape[-1])`

3. **通道选择**
   - 当前代码使用第一个通道：`feat_pred[:, 0:1]`
   - 如需对所有通道计算，可以使用循环（见使用方法1的代码）

---

## 🔧 测试验证

### 测试 Axial Attention
```bash
cd /home/bobbystone/CKAAD
python -c "from model.AxialAttention import AxialAttention; import torch; x = torch.randn(2, 512, 16, 16); m = AxialAttention(dim=512); y = m(x); print(f'测试通过: {x.shape} -> {y.shape}')"
```

### 测试 SSIM 损失
```bash
cd /home/bobbystone/CKAAD
python test_ssim.py
```

---

## 📊 预期效果

### Axial Attention
- **增强点**: patch间的长程依赖关系，特别是行列方向的结构连续性
- **适用场景**: 条带状结构、连通区域、边缘连续性要求高的任务

### SSIM 损失
- **增强点**: 对结构模式（边缘、纹理）的感知，而不仅仅是像素级相似度
- **预期提升**: 
  - 边缘更清晰
  - 结构完整性更好
  - 对局部结构错误更敏感

---

## ⚠️ 注意事项

1. **计算开销**
   - Axial Attention 的计算复杂度为 O(HW(H+W))，在中等分辨率特征图上可接受
   - SSIM 损失计算相对较快，但如果对所有层级和所有通道计算，会显著增加训练时间

2. **训练稳定性**
   - 如果同时启用多个新模块，建议先逐个测试
   - SSIM损失的权重（lambda_ssim）建议从小值开始，逐步增大

3. **特征图尺寸**
   - SSIM要求特征图尺寸 >= window_size（默认11）
   - 如果特征图太小，代码会自动上采样

4. **与现有模块的配合**
   - Axial Attention 与 CoordAtt 形成互补：
     - CoordAtt: "硬权重"（池化+卷积），行列方向的全局信息
     - Axial Attention: "软权重"（注意力机制），行列方向的长程依赖
   - ECA注意力负责通道维度的特征选择

---

## 📚 相关论文

1. **Axial Attention**: "Axial Attention in Multidimensional Transformers" (Ho et al., ICML 2019)
2. **SSIM**: "Image Quality Assessment: From Error Visibility to Structural Similarity" (Wang et al., TIP 2004)

---

## 🐛 问题排查

如果遇到问题：

1. **Import错误**: 确保 `model/AxialAttention.py` 文件存在
2. **维度不匹配**: 检查特征图的通道数是否与Axial Attention的dim参数匹配
3. **SSIM计算错误**: 确保特征图尺寸 >= window_size，或使用上采样
4. **训练不稳定**: 减小SSIM权重，或先只启用Axial Attention

---

## ✅ 完成清单

- [x] Axial Attention 模块实现
- [x] Axial Attention 集成到 Encoder
- [x] SSIM 损失函数实现
- [x] SSIM 使用说明和示例代码
- [x] 测试验证
- [x] 使用文档


